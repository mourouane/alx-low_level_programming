#include "main.h"
/**
* add - adds two integers and returns the result
* @a: input number1
* @b: input number2
* Return: sum of a and b
*/
int add(int a, int b)
{
int sum;
sum = a + b;
return (sum);
}
